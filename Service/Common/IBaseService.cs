using System.Collections.Generic;

namespace Service.Common
{
    public interface IBaseService : ISave
    {
        //ReturnModel<T> Create<T>(T entity);
        //ReturnModel<T> Update(T entity);
        //ReturnModel<IEnumerable<T>> GetAll();
        //ReturnModel<T> Get(int Id);
        //ReturnModel<int> Delete(T entity);
    }
}
